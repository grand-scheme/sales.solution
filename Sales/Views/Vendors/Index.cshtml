@{
  Layout = "_Layout";
}
@using Sales.Models;

<h1> 
  [DUMMY TITLE TEXT // VENDOR INDEX] 
</h1>

@if (@Model.Count == 0)
{
  <p> 
    [ blank space bc blank vendors ] 
  </p>
}

@foreach (Vendor vendor in Model)
{
  <p>
    <a href="/vendors/@vendor.Id">
      @vendor.Name
    </a>
  </p>
}

<a href="/">Back to home</a>

@* Footer HTML *@